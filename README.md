# Apollo v2

Apollo is a secure CLI utility for running Windows, Android, and macOS applications on Linux.

## Supported
- Windows (.exe) via Wine + LXD
- Android (.apk) via Waydroid
- macOS (.app/.dmg) via Darling

## Repository
Local JSON repository included in repo/repo.json

## Install
sudo ./install.sh

